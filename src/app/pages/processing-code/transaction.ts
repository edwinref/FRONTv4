export interface TransactionType {
     id:number;
    code: string;
    description: string;
  deleting?: boolean;

}
export interface TransactionStatue {
  id:number;
    code: string;
    description: string;
  deleting?: boolean;

}

