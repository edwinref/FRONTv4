export interface ProcessingCode {
    code: string;
    description: string;
    deleting?: boolean;

}
export interface MessageType {
    code: string;
    description: string;
  deleting?: boolean;

}

